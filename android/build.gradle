plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.13'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'trivia-api'
            version = '1.1.13'

            from components.java

            pom {
                name = 'Trivia Generator'
                description = 'Trivia is a simple tool for getting trivia questions. It returns a random trivia question based on your selected category.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/trivia-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/trivia-api.git'
                    url = 'https://github.com/apiverve/trivia-api'
                }
            }
        }
    }
}
